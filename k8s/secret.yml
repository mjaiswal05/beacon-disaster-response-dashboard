apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: beacon-dashboard-secrets
  namespace: ${NAMESPACE}
data:
  # Add any frontend secrets here if needed (e.g., API keys)
  # Example: api-key: base64-encoded-value
